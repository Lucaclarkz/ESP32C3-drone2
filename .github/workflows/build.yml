name: Build ESP32-C3 Firmware

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Arduino CLI
        run: |
          curl -fsSL https://downloads.arduino.cc/arduino-cli/arduino-cli_latest_Linux_64bit.tar.gz -o arduino-cli.tar.gz
          tar -xzf arduino-cli.tar.gz
          sudo mv arduino-cli /usr/local/bin/
          arduino-cli version

      - name: Configure CLI
        run: |
          arduino-cli config init
          arduino-cli config set board_manager.additional_urls https://espressif.github.io/arduino-esp32/package_esp32_index.json

      - name: Install ESP32 Core
        run: |
          arduino-cli core update-index
          arduino-cli core install esp32:esp32@2.0.17

      - name: Install Libraries (Correct Names)
        run: |
          arduino-cli lib update-index
          arduino-cli lib install AsyncTCP@1.1.1
          arduino-cli lib install ESPAsyncWebServer@1.2.3
          arduino-cli lib install Adafruit_MPU6050@2.2.9
          arduino-cli lib install Adafruit_Unified_Sensor@1.1.15
          arduino-cli lib install Adafruit_BusIO@1.17.4

      - name: Prepare Sketch
        run: |
          mkdir -p sketch/drone
          cp drone/drone.ino sketch/drone/drone.ino

      - name: Compile ESP32-C3
        run: |
          arduino-cli compile \
            --fqbn esp32:esp32:esp32c3 \
            --output-dir build \
            sketch/drone

      - name: Upload BIN
        uses: actions/upload-artifact@v4
        with:
          name: firmware
          path: build/*.bin
